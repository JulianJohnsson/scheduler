<h1>Session#new</h1>
<%= simple_form_for :login, { url: sessions_path } do |f| %>
	<%= f.input :email %>
	<%= f.input :password %>
	<%= f.check_box :remember_me %>
	<%= f.button :submit %>
<% end %>
<p>
	<%= link_to "forgotteb password ?", new_password_reset_path %>
</p>